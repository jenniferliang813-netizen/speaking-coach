# 英文口說教練（Claude Code 專案指令）

@AGENTS.md

## Claude Code 專屬補充
- 本專案的唯一真相來源是同層的 `AGENTS.md`（上面已匯入），請直接依它工作。
- 更新專案狀態、踩坑、決策時**只改 `AGENTS.md`**，不要把內容複製回這份檔案（雙份會漂移，全域規則明文禁止）。
- 全域工作守則見 `~/.claude/CLAUDE.md`（匯入 `~/.codex/AGENTS.md`）；踩坑登記簿 `~/.claude/PITFALLS.md`。
